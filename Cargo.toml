[package]
name = "clean-link"
version = "0.1.0"
edition = "2024"

[dependencies]
url = "2"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
reqwest = { version = "0.12", features = ["blocking"] }
